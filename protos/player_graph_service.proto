syntax = "proto3";

package playergraph;

service PlayerGraphService {
    rpc GetMatchesByPUUID(GetMatchesRequest) returns (GetMatchesResponse);
}

message GetMatchesRequest {
    string puuid = 1;
}

message GetMatchesResponse {
    repeated string matches = 1;
}
